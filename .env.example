# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
GMAIL_USER=gameshopenter@gmail.com
GMAIL_APP_PASSWORD=your_gmail_app_password_here

# Database / Storage (Netlify Blobs)
# No additional configuration needed - uses Netlify secrets

# Payment Provider (Mollie)
# Configure in Netlify environment variables

# Google Analytics
NEXT_PUBLIC_GA_ID=G_xxxxxxxxxx

# Node Environment
NODE_ENV=production
